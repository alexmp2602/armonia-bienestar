---
const { delay = 0, y = 24 } = Astro.props;

const delayMs = `${Number(delay) * 500}ms`;
const yPx = `${Number(y)}px`;
---

<div data-reveal style={`--reveal-delay: ${delayMs}; --reveal-y: ${yPx};`}>
  <slot />
</div>
